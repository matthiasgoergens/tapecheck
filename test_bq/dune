(tests
 (names test_roundtrip test_shrink test_wrapper test_fn_shrink)
 (build_if
  (<> %{profile} oxcaml))
 (libraries base base_quickcheck splittable_random tape tape_engine)
 (preprocess
  (pps ppx_quickcheck ppx_sexp_conv ppx_compare ppx_let)))
