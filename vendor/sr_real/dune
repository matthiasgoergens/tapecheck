; Jane Street's splittable_random v0.17 implementation, vendored with
; two edits: a Base v0.18 (OxCaml overlay) compat block at the top of
; sr_real.ml, and upstream's inline test/bench blocks stripped (they
; use APIs that drifted in v0.18; the originals remain in the release
; tarball for reference).
(library
 (name sr_real)
 (libraries base)
 (preprocess
  (pps ppx_sexp_message)))
