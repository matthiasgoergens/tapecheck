; Jane Street's base_quickcheck v0.17.1 plus the reviewed patch recorded in
; vendor/patches/. Within this workspace `splittable_random` resolves to our
; tape shim; Wave 2 also adds opt-in structural-list and explicit-leaf-budget
; generators while leaving the legacy defaults unchanged.
(library
 (name base_quickcheck)
 (libraries base splittable_random)
 (preprocess
  (pps ppx_base ppx_fields_conv ppx_let ppx_sexp_message ppx_sexp_value)))
